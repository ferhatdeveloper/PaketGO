# PostgREST Configuration for PaketGO

db-uri = "postgres://paketgo:paketgo@localhost:5432/paketgo"
db-schemas = "public"
db-anon-role = "web_anon"

server-host = "0.0.0.0"
server-port = 3000

jwt-secret = "super-secret-jwt-token-for-paketgo-app-dev"

# CORS
# server-cors-allowed-origins = "*"
